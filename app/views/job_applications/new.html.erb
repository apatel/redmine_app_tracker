<% form_for(@job_application) do |f| %>
  <%= f.error_messages %>
  <%= f.hidden_field(:apptracker_id, :value => @apptracker.id ) %>

  <%
    # FIXME Implement an Ajax interface that allows the user to input an :application_material_count, generating a data input table with a row count matching the value found in :application_material_count 
  %>

  
  <p>
    <%= f.label(:application_material_count) %><br />
    <%= f.text_field(:application_material_count) %>
  </p>


  <p>
  TODO: Add AJAX for selecting number and type of application materials needed
  </p>


  <p>
  TODO: Add AJAX for 1) uploading 0+ files or 2) selecting from an existing list of uploaded files
  </p>

  <p>
    <%= f.label(:referrer_count) %><br />
    <%= f.text_field(:referrer_count, :size => 3) %>
  </p>

  <p>
    <%= f.submit('Save') %>
  </p>
<% end %>

