<% flash[:notice] %>
<h2><%=h session[:apptracker_title] %>: <%=h "#{@applicant.first_name} #{@applicant.last_name}" %>: Referrers </h2>

<%= render :partial => "shared/tabs" %>

<%= render :partial => "referrer_tables" %>

<br /><br />

<% if(User.current.admin? || User.current.logged?) %>
  <%= link_to 'New Referrer', new_referrer_url %>
<% end %>
