<% flash[:notice] %>
<div class="contextual">
  <% if (User.current.admin? || User.current.logged?) %>
    <%= link_to('New Referrer', new_referrer_url, :class => 'icon icon-add') %> |
  <% end %>
  <%= link_to 'Return to AppTracker List', apptrackers_url(:project_identifier => Apptracker.find(params[:apptracker_id]).project.identifier), :class => 'icon icon-history' %>
</div>

<h2><%=h Apptracker.find(params[:apptracker_id]).title %>: <%=h "#{@applicant.first_name} #{@applicant.last_name}" %>: Referrers </h2>

<%= render :partial => "shared/tabs" %>

<%= render :partial => "referrer_tables" %>

<br /><br />

