<% flash[:notice] %>
<div class="contextual">
  <% if(User.current.admin? || User.current.logged?) %>
    <%= link_to 'New Application Material', new_application_material_url(:apptracker_id => params[:apptracker_id]), :class => 'icon icon-add'%> |
  <% end %>
  <%= link_to 'Return to AppTracker List', apptrackers_url(:project_identifier => Apptracker.find(params[:apptracker_id]).project.identifier), :class => 'icon icon-history' %>
</div>
<h2><%=h Apptracker.find(params[:apptracker_id]).title %>: Application Materials</h2>

<%= render :partial => 'shared/tabs' %>

<%= render :partial => 'application_material_tables' %>

<br /><br />
