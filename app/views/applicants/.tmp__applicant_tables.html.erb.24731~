<table class="list">
<thead>
  <tr>
    <% if(User.current.admin?)%>
      <!-- Admin Role -->
      <th style="width:50%; text-align:left;">Name</th>
      <th style="width:40%; text-align:left;">User Name</th>
      <th style="width:5%; text-align:left;"></th>
      <th style="width:5%; text-align:left;"></th>

    <% elsif (User.current.logged?)%>
      <!-- Applicant Role -->
      <!-- No view for applicants -->
    <% else %>
      <!-- Anonymous Role -->
      <!-- No view for Anonymous -->

    <% end %> 
  </tr>
</thead>
<tbody>
  <% @applicants.each do |applicant| %>
    <tr>
      <% if(User.current.admin?)%>
        <!-- Admin Role -->
        <td style="width:50%"><%= link_to "#{applicant.first_name} #{applicant.last_name}", applicant_url(applicant.id, :apptracker_id => params[:apptracker_id]) %></a></td>
      	<td style="width:40%"><%=h applicant.user_name%></a></td>
    	<td style="width:5%"><%= link_to 'Edit', edit_applicant_url(applicant.id, :apptracker_id => params[:apptracker_id]), :class => 'icon icon-edit' %></td>
      	<td style="width:5%"><%= link_to 'Delete', applicant_url(:id => applicant.id, :apptracker_id => params[:apptracker_id]), 
        :confirm => "Are you sure you want to delete #{applicant.first_name} #{applicant.last_name}\'s record?", 
        :method => :delete,
        :class => 'icon icon-del' %>
      </td>

      <% elsif (User.current.logged?)%>
        <!-- Applicant Role -->
        <!-- No view for Applicants -->

      <% else %>
        <!-- Anonymous Role -->
        <!-- No view for Anonymous -->

      <% end %> 
    </tr>
  <% end %>
</tbody>
</table>

